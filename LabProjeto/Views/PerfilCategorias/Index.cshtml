@model IEnumerable<LabProjeto.Models.PerfilCategoria>

<h1>Lista de Categorias Favoritas</h1>

<p>
    <a class="btn btn-dark pb-2"  asp-action="Create">Adicionar Categoria</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Nome
            </th>
            <th>
                Descrição
            </th>
            <th></th>
        </tr>
    </thead>
 <tbody>
@foreach (var item in Model){
            <tr>
                <td>
                    @Html.DisplayFor(modelItem =>item.categoria.Nome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.categoria.Descricao)
                </td>
                <td>

                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-dark pb-2">Apagar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
